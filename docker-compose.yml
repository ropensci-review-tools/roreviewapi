version: "3.7"
services:
  nginx:
    image: nginx
    volumes:
      - $PWD/nginx.conf:/tmp/nginx.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro
    environment:
      - PLUMBER_SERVER_ADDR=plumber:8000
      - NGINX_SERVER_NAME=reviewbot.ropensci.org
    command: /bin/bash -c "envsubst '$${PLUMBER_SERVER_ADDR} $${NGINX_SERVER_NAME}' < /tmp/nginx.conf > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
    ports:
      - 80:80
      - 443:443
    depends_on:
      plumber:
        condition: service_healthy
  plumber:
    build: .
    restart: always
    volumes:
      - /srv/roreviewapi/data:/data/email
    environment:
      - ROREVIEWAPI_EMAIL_DB=/data/email/searches.sqlite
      - GMAIL_SENDER=${GMAIL_SENDER}
      - GMAIL_OAUTH_CLIENT_SECRET=${GMAIL_OAUTH_CLIENT_SECRET}
      - GMAIL_TOKEN_CACHE=${GMAIL_TOKEN_CACHE}
      - GMAIL_AUTH_EMAIL=${GMAIL_AUTH_EMAIL}
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8000' 2>/dev/null || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 120s
