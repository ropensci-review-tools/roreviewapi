resolver 127.0.0.11 valid=10s;

server {
  listen 80;
  location / {
    set $upstream http://${PLUMBER_SERVER_ADDR};
    proxy_pass $upstream;
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;
    proxy_connect_timeout 10s;
  }
}
